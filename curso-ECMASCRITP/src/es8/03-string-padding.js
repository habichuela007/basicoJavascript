//método para trabajar con string, padding = relleno
const string = 'Hello';

console.log(string.padStart(6,'_')); //cuánto agregamos el relleno, qué relleno agregamos
console.log(string.padEnd(6,'_')); //cuánto agregamos el relleno, qué relleno agregamos